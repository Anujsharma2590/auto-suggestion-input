export type UserDataType = {
  id: string;
  name: string;
  items: string[];
  address: string;
  pincode: string;
};

export enum KeyCodes {
  ENTER = "Enter",
  ARROW_DOWN = "ArrowDown",
  ARROW_UP = "ArrowUp",
}
